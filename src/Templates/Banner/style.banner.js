import styled from "styled-components"
import resturent from "../../assests/resturnt.jpg";

const StyleBanner = styled.article`
    background: #ffffff url(${resturent}) no-repeat center;
    background-size: cover;
    width: 100%;
`

export default StyleBanner