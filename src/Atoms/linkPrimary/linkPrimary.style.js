export const StylelinkPrimary = (param) => {
    return {
        fontSize: param.fontSize || "20px",
        fontWeight: param.fontWeight,
        textDecoration: "none",
        color: "black",
    };
};