import styled from "styled-components";

export const Stylelocations = styled.ul`
    list-style-type: none;
    display: grid;
    grid-template-columns: auto auto auto;
    gap: 30px 20px;
    padding: 0
`
